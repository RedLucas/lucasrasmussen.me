# Unsplash API access key, used for the random background image.
# Create an app at https://unsplash.com/oauth/applications to get one.
#
# Client-side keys are visible in the built bundle by design — only the access
# key belongs here. Never put the app's *secret* in this file.
VITE_UNSPLASH_ACCESS_KEY=
